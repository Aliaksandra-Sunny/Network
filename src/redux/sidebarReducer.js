let initialState = {
    friends: [
        {id: 1, name: 'Tolya'},
        {id: 2, name: 'Tanya'},
        {id: 3, name: 'Nastya'},
    ],
}

const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;